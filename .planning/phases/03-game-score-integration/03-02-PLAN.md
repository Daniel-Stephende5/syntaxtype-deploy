---
phase: 03-game-score-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/components/games/typing/TypingTest.js
  - src/components/games/typing/FallingTypingTest.js
autonomous: true
requirements:
  - SCORE-01
  - SCORE-02
must_haves:
  truths:
    - "TypingTest.js submits to /api/scores/TYPING_TESTS after game completion"
    - "FallingTypingTest.js submits to /api/scores/FALLING_WORDS after game completion"
    - "Loading state shown during submission"
    - "User can retry if submission fails"
  artifacts:
    - path: "src/components/games/typing/TypingTest.js"
      provides: "Score submission to TYPING_TESTS category"
    - path: "src/components/games/typing/FallingTypingTest.js"
      provides: "Score submission to FALLING_WORDS category"
  key_links:
    - from: "TypingTest.js"
      to: "/api/scores/TYPING_TESTS"
      via: "fetch POST"
      pattern: "fetch.*api/scores"
---

<objective>
Update TypingTest.js and FallingTypingTest.js to submit scores to the unified leaderboard endpoint.
</objective>

<context>
@.planning/phases/03-game-score-integration/03-CONTEXT.md
@.planning/phases/03-game-score-integration/03-01-SUMMARY.md
@.planning/REQUIREMENTS.md

**Phase requirement IDs:** SCORE-01, SCORE-02

## Frontend Submission (from CONTEXT.md)
- Manual submit button after game completion
- Show spinner during submission
- Allow retry if submission fails
- Extract JWT from localStorage/cookie at submission time
</context>

<tasks>

<task type="auto">
  <name>task 1: Update TypingTest.js to submit to leaderboard</name>
  <files>src/components/games/typing/TypingTest.js</files>
  <action>
Update TypingTest.js:

1. After game completion, show "Submit Score" button (or auto-submit on results screen)
2. Create submitScore() function that:
   - Gets JWT token from localStorage
   - If no token: show "Login to save score" message, allow play anyway
   - If token: POST to /api/scores/TYPING_TESTS with {wpm, accuracy, score}
   - Show loading spinner during fetch
   - On success: display "Score submitted! New Best!" or "Score submitted! Rank: X"
   - On error: show error toast, allow retry button
3. Preserve existing /api/scores submission if still needed for analytics

Category: TYPING_TESTS
Score: Use combined score = wpm * (accuracy/100) * (accuracy > 95 ? 1.5 : 1)
  </action>
  <verify>
<automated>grep -n "api/scores/TYPING_TESTS" src/components/games/typing/TypingTest.js</automated>
  </verify>
  <done>TypingTest.js submits to /api/scores/TYPING_TESTS with correct payload</done>
</task>

<task type="auto">
  <name>task 2: Update FallingTypingTest.js to submit to leaderboard</name>
  <files>src/components/games/typing/FallingTypingTest.js</files>
  <action>
Update FallingTypingTest.js:

1. After game completion, show "Submit Score" button
2. Create submitScore() function similar to TypingTest.js
3. POST to /api/scores/FALLING_WORDS with {wpm, accuracy, score}
4. Show loading state during submission
5. Handle success/error responses with appropriate UI
6. Allow retry on failure

Category: FALLING_WORDS
  </action>
  <verify>
<automated>grep -n "api/scores/FALLING_WORDS" src/components/games/typing/FallingTypingTest.js</automated>
  </verify>
  <done>FallingTypingTest.js submits to /api/scores/FALLING_WORDS with correct payload</done>
</task>

</tasks>

<verification>
[Frontend verification]
- Play TypingTest, complete game, submit score, check /leaderboard shows entry under TYPING_TESTS
- Play FallingTypingTest, complete game, submit score, check /leaderboard shows entry under FALLING_WORDS
</verification>

<success_criteria>
[Measurable completion]
- Both typing games submit to correct category endpoints
- Loading state shown during submission
- Error handling with retry option
- Scores appear on leaderboard after submission
</success_criteria>

<output>
After completion, create `.planning/phases/03-game-score-integration/03-02-SUMMARY.md`
</output>
