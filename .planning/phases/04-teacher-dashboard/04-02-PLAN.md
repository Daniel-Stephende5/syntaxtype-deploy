---
phase: 04-teacher-dashboard
plan: '02'
type: execute
wave: 2
depends_on:
  - '04-01'
files_modified:
  - frontend/src/pages/TeacherDashboard.js
  - frontend/src/App.js
autonomous: true
requirements:
  - TCHR-01
  - TCHR-03
---

<objective>
Create frontend Teacher Dashboard page with student list and class leaderboard.
</objective>

<context>
@.planning/phases/04-teacher-dashboard/04-CONTEXT.md
@frontend/src/pages/LeaderboardPage.js
@frontend/src/components/ProtectedRoute.js
@frontend/src/utils/api.js

<!-- From Plan 01 (backend contract) -->
<!-- API Contract: GET /api/teacher/students returns List<StudentProgressDTO> -->
<!-- API Contract: GET /api/teacher/leaderboard returns List<LeaderboardEntry> -->
</context>

<tasks>

<task type="auto">
  <name>task 1: Create TeacherDashboard.js page</name>
  <files>frontend/src/pages/TeacherDashboard.js</files>
  <action>
Create new TeacherDashboard.js page component:

1. State: students (array), leaderboard (array), loading (bool), error (string)

2. useEffect on mount: 
   - Fetch GET /api/teacher/students 
   - Fetch GET /api/teacher/leaderboard
   
3. Render:
   - Page title "Teacher Dashboard"
   - Tab/Toggle: "Students" | "Leaderboard" (use useState for active tab)
   
4. Students Tab:
   - Grid of cards (use MUI Card or similar)
   - Each card: student username, email, totalGamesPlayed, averageWpm, averageAccuracy
   - Click card navigates to /teacher/students/{id}
   - Empty state: "No students found"
   
5. Leaderboard Tab:
   - Reuse table structure from LeaderboardPage.js
   - Columns: Rank, Username, WPM, Accuracy, Combined Score, Game, Date
   - Show only students (not filtered by game = all)
   
6. Loading: Show MUI CircularProgress spinner
7. Error: Show error message with retry button

Use axios for API calls with Authorization header from localStorage. Handle 401/403 with redirect to login.
  </action>
  <verify>
<automated>ls -la frontend/src/pages/TeacherDashboard.js</automated>
  </verify>
  <done>TeacherDashboard.js exists with working fetch to /api/teacher/* endpoints</done>
</task>

<task type="auto">
  <name>task 2: Add routes to App.js</name>
  <files>frontend/src/App.js</files>
  <action>
Add routes in App.js for teacher dashboard:

1. Import TeacherDashboard:
```javascript
import TeacherDashboard from './pages/TeacherDashboard';
```

2. Add route inside the router (after other TEACHER routes, around line 88-100):
```jsx
<ProtectedRoute allowedRoles={['TEACHER', 'ADMIN']} path="/teacher/dashboard" element={<TeacherDashboard />} />
```

Add to Navbar/sidebar navigation if exists (look for Dashboard link pattern).
  </action>
  <verify>
<automated>grep -n "teacher/dashboard" frontend/src/App.js</automated>
  </verify>
  <done>/teacher/dashboard route exists and is protected for TEACHER, ADMIN roles</done>
</task>

</tasks>

<verification>
- [ ] TeacherDashboard.js renders without errors
- [ ] Fetches from /api/teacher/students and /api/teacher/leaderboard
- [ ] Students tab shows card grid
- [ ] Leaderboard tab shows table
- [ ] Route /teacher/dashboard is protected
</verification>

<success_criteria>
Teacher can view:
- List of all students with summary stats (games played, avg WPM, avg accuracy)
- Class leaderboard sorted by score

Both views accessible at /teacher/dashboard with proper role protection.
</success_criteria>

<output>
After completion, create `.planning/phases/04-teacher-dashboard/04-02-SUMMARY.md`
</output>
