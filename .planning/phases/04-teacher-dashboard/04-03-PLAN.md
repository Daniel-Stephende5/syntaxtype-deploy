---
phase: 04-teacher-dashboard
plan: '03'
type: execute
wave: 3
depends_on:
  - '04-02'
files_modified:
  - frontend/src/pages/TeacherStudentDetail.js
  - frontend/src/App.js
autonomous: true
requirements:
  - TCHR-02
  - TCHR-04
---

<objective>
Create individual student detail view showing comprehensive progress data.
</objective>

<context>
@.planning/phases/04-teacher-dashboard/04-CONTEXT.md
@frontend/src/pages/TeacherDashboard.js
@frontend/src/pages/StudentStatisticsPage.js

<!-- From Plan 01 (backend contract) -->
<!-- API Contract: GET /api/teacher/students/{id} returns StudentProgressDTO -->
</context>

<tasks>

<task type="auto">
<name>task 1: Create TeacherStudentDetail.js page</name>
<files>frontend/src/pages/TeacherStudentDetail.js</files>
<action>
Create new TeacherStudentDetail.js page component:

1. Get studentId from useParams (react-router-dom)

2. State: student (StudentProgressDTO), loading (bool), error (string)

3. useEffect: Fetch GET /api/teacher/students/{studentId}

4. Render:
   - Back button linking to /teacher/dashboard
   - Header: "Student Progress: {username}"
   - Subheader: {email}
   
5. Summary Cards Row:
   - Total Games Played (count)
   - Average WPM (with icon)
   - Average Accuracy (with icon)
   
6. Best Scores Section:
   - Table showing best score per game category
   - Columns: Game Category, Best Score, WPM, Accuracy, Date
   - Use bestScores map from API response
   
7. Recent Activity Section (if available):
   - List of recent attempts
   - Show last 10 entries
   
8. Loading: MUI CircularProgress
9. Error: Error message with "Back to Dashboard" button
10. 404: "Student not found" message

Use axios with auth header. Handle 401/403 redirect to login.
  </action>
  <verify>
<automated>ls -la frontend/src/pages/TeacherStudentDetail.js</automated>
  </verify>
  <done>TeacherStudentDetail.js exists with fetch to /api/teacher/students/{id}</done>
</task>

<task type="auto">
<name>task 2: Add student detail route to App.js</name>
<files>frontend/src/App.js</files>
<action>
Add route for individual student detail:

1. Import TeacherStudentDetail:
```javascript
import TeacherStudentDetail from './pages/TeacherStudentDetail';
```

2. Add route:
```jsx
<ProtectedRoute allowedRoles={['TEACHER', 'ADMIN']} path="/teacher/students/:id" element={<TeacherStudentDetail />} />
```

Add near the /teacher/dashboard route (after it in sequence).
  </action>
  <verify>
<automated>grep -n "teacher/students" frontend/src/App.js</automated>
  </verify>
  <done>/teacher/students/:id route exists and is protected</done>
</task>

</tasks>

<verification>
- [ ] TeacherStudentDetail.js renders without errors
- [ ] Fetches student data from /api/teacher/students/{id}
- [ ] Shows summary cards (games, WPM, accuracy)
- [ ] Shows best scores by category table
- [ ] Back navigation works to dashboard
- [ ] Route protected for TEACHER, ADMIN
</verification>

<success_criteria>
Teacher can view individual student:
- Profile info (username, email)
- Summary stats (total games, avg WPM, avg accuracy)
- Best scores by game category
- Recent activity history (if available)

Accessible via clicking student card on dashboard.
</success_criteria>

<output>
After completion, create `.planning/phases/04-teacher-dashboard/04-03-SUMMARY.md`
</output>
